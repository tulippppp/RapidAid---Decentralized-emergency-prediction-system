{
  "name": "rapidaid-emergency-prediction-system",
  "version": "1.0.0",
  "private": true,
  "description": "RapidAid smart contracts and deployment tooling",
  "scripts": {
    "chain": "hardhat node",
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:shardeum": "hardhat run scripts/deploy.js --network shardeumTestnet",
    "frontend:build": "cd frontend && npm run build",
    "seed:demo:local": "hardhat run scripts/seed-demo.js --network localhost",
    "seed:demo:shardeum": "hardhat run scripts/seed-demo.js --network shardeumTestnet"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17"
  }
}
